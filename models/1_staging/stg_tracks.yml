version: 2  
  
models:  
  - name: stg_tracks
    columns:
      - name: track_id
        description: Staged data for track_id.
        tests:
          - not_null
          - unique
      - name: artist_id
        description: Staged data for artist_id.
        tests:
          - not_null
